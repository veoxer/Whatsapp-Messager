# Generate a long random value before running Docker Compose.
# PowerShell example:
#   -join ((48..57 + 65..90 + 97..122) | Get-Random -Count 48 | ForEach-Object {[char]$_})
API_KEY=replace-this-with-at-least-32-random-characters
